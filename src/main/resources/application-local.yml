spring:
  datasource:
    url: jdbc:mysql://localhost:3307/driving_reservation?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/schema.sql
      data-locations: classpath:sql/data.sql

logging:
  level:
    com.example.driving: DEBUG
    org.springframework.jdbc: DEBUG
