# Grafana 기동 시 Prometheus 데이터소스를 자동 연결(수동 설정 불필요)
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus               # 대시보드 JSON에서 이 uid로 참조
    access: proxy
    url: http://prometheus:9090   # 같은 docker 네트워크 내 서비스명으로 접근
    isDefault: true
    editable: true
